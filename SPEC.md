# Ulike AirPro S 가격 감시 — 사양서

Claude Code 인계용. 기존 `watch.py`가 있으면 출발점으로 쓰되, ATL(역대 최저가) 로직이
없으므로 상태 관리 부분은 재설계 필요.

---

## 0. 한 줄 요약

楽天市場 / Yahoo!ショッピング에서 **Ulike AirPro S** 실질가를 하루 3회 수집하고,
**역대 최저가를 갱신하면** Gmail로 알린다. 이력은 누적해서 그래프로 본다.

---

## 1. 감시 대상

### 1.1 상품

| 항목 | 값 |
|---|---|
| 모델 | Ulike AirPro S (2025년 모델, 品番 UI04S) |
| 정가 | 49,800엔 (세금 포함) |
| 참고 실적 | 26,892엔 (2026-07, 라쿠텐 46%OFF 쿠폰) |

단일 대상. 다른 모델은 추가하지 않는다(노이즈 회피). 단, `TARGETS` 구조는 배열로
유지해서 나중에 늘릴 수 있게 한다.

### 1.2 수집처

| 사이트 | 방식 | 채택 |
|---|---|---|
| 楽天市場 | 商品検索API v2 (`IchibaItem/Search/20220601`) | O |
| Yahoo!ショッピング | 商品検索API v3 (`V3/itemSearch`) | O |
| Amazon | 공식 API 없음, 체크박스 쿠폰은 파싱 불가 | X (Keepa 별도 운용) |
| メルカリ | 공식 API 없음, 자동수집 약관 위반 | X |

### 1.3 "실질가" 정의

API의 `itemPrice` / `price`는 **쿠폰 적용 전 정가**를 반환하는 경우가 대부분이다.
Ulike 공식점은 상품명에 쿠폰가를 써넣으므로 이를 파싱한다.

```
＼クーポンで29,120円！7/27 19:00-7/30／Ulike 公式 IPL光美容器 AirPro S ...
【46%OFFクーポンで49,800円 → 26,892円！7/4〜7/12】Ulike 公式 ...
```

**실질가 = min(API 가격, 상품명에서 파싱한 쿠폰가)**

파싱 패턴(우선순위 무관, 매칭된 것 중 최솟값 채택):

```
クーポン(?:利用|適用)?で\s*([\d,]+)\s*円
→\s*([\d,]+)\s*円
([\d,]+)\s*円\s*[!！]
実質\s*([\d,]+)\s*円
```

**스코프 아웃**: 포인트 환원(라쿠텐 SPU, PayPay)은 실질가에 반영하지 않는다.
조건이 계정마다 달라서 일반화 불가. 사람이 최종 판단할 몫으로 남긴다.

---

## 2. 알림 조건 — 역대 최저가(ATL) 갱신

### 2.1 기본 규칙

- ATL은 **사이트 통합**으로 하나만 관리한다(어디서 사든 싼 게 이득이므로).
  단 `history`에는 사이트별로 기록해서 그래프에서 분리해 볼 수 있게 한다.
- 현재 최저가 < ATL 이면 ATL을 갱신하고 알림.

### 2.2 콜드 스타트

첫 실행은 이력이 없어서 무조건 "최저가 갱신"이 된다. 그러므로:

- **첫 실행은 baseline 기록만 하고 가격 알림은 보내지 않는다.**
- 대신 "baseline 설정 완료 / 현재 최저가 X엔" 메일 1통을 보내 동작 확인을 겸한다.

### 2.3 미세 갱신 억제

26,892엔 → 26,880엔(12엔 차) 같은 갱신으로 메일이 오면 노이즈다.

- **갱신폭이 500엔 미만이면 ATL은 갱신하되 메일은 보내지 않는다.**
- 즉 `ATL - current >= 500` 일 때만 통지.
- 상수 `MIN_DROP_YEN = 500` 으로 빼둘 것.

### 2.4 ATL 오염 대비 (중요)

오탐으로 ATL이 비정상적으로 낮게 박히면(예: 케이스를 본체로 오인해 3,000엔 기록)
**이후 영원히 알림이 안 온다.** 이게 이 설계의 가장 큰 단일 실패 지점이다.

대응:
- 아래 §4의 필터를 반드시 통과시킬 것
- CLI로 수동 복구 가능하게:
  ```
  python watch.py --show-atl
  python watch.py --reset-atl              # ATL 삭제, 다음 실행에서 재설정
  python watch.py --set-atl 29800          # 수동 지정
  ```
- 알림 메일에 **상품명 전문과 URL을 반드시 포함**해서 사람이 오탐을 즉시 알아채게 한다

---

## 3. 확인 수단 (4가지 전부 구현)

### 3.1 Gmail 알림

- 발송: `smtplib` + `smtp.gmail.com:465` (SSL), 앱 패스워드 인증
- 본문에 반드시 포함: 실질가 / 정가 / 인하폭 / 이전 ATL / **상품명 전문** / 스토어명 / URL / 수집 시각(JST)
- text + HTML 멀티파트
- 제목 예: `【最安更新】Ulike AirPro S 26,892円 (楽天) ▼2,228円`

발송 트리거는 3종:
1. ATL 갱신 (§2)
2. 첫 실행 baseline 설정
3. 헬스 경고 (§4.4)

### 3.2 Actions 로그 매회 기록

매 실행마다 stdout에 남긴다:

```
[2026-09-08 09:00 JST] Ulike AirPro S
  楽天  : 実質 31,860円 (定価 49,800円) / 候補 12件 / クーポン解析成功 9件
  Yahoo : 実質 32,800円 (定価 49,800円) / 候補  8件 / クーポン解析成功 6件
  最安  : 31,860円 (楽天)
  ATL   : 26,892円 (2026-07-05)  → 更新なし
```

후보 건수와 파싱 성공 건수를 항상 찍을 것. 파싱이 조용히 죽는 걸 잡는 유일한 단서다.

### 3.3 이력 누적 + 그래프 대시보드

**데이터**
- `docs/history.jsonl` — append-only, 1행 1레코드(JSONL). 머지 충돌 회피용.
  ```json
  {"ts":"2026-09-08T09:00:00+09:00","site":"rakuten","price":31860,"list":49800,"shop":"ULIKE CARE","url":"https://...","name":"..."}
  ```
- 실행당 사이트별 최저 1건씩만 기록. 3회/일 × 2사이트 = 연 약 2,200행. 용량 문제 없음.

**대시보드**
- `docs/index.html` — 정적 HTML. Chart.js를 CDN에서 로드, `history.jsonl`을 fetch해서 렌더.
- 표시 항목:
  - 라쿠텐 / 야후 각각의 실질가 시계열 (라인 2개)
  - ATL 수평 보조선
  - 정가 수평 보조선
  - 최근 30일 / 90일 / 전체 토글
- GitHub Pages: Settings → Pages → Source를 `main` 브랜치 `/docs`로 지정
- 인증 없는 공개 페이지가 되므로 **비밀 정보는 절대 넣지 말 것**

### 3.4 수동 실행

- **로컬**: `python watch.py --dry-run` — 메일 안 보내고 현재 시세만 출력
- **원격**: `workflow_dispatch` 로 Actions 탭에서 수동 트리거. 입력 파라미터로 `dry_run` 제공

---

## 4. 한계와 예외 처리

### 4.1 검지 불가 케이스 (사양상 인정)

| 케이스 | 결과 | 대응 |
|---|---|---|
| 상품명에 쿠폰가를 안 쓰는 스토어 | 정가로만 인식 | 사양상 허용. 놓칠 수 있음을 README에 명기 |
| Amazon 체크박스 쿠폰 | 수집 불가 | Keepa 병행 (스코프 외) |
| 타임세일/쇼핑몰 전체 쿠폰 | 미반영 | 사양상 허용 |
| 포인트 환원 | 미반영 | §1.3 참조 |

### 4.2 오탐 필터 (필수)

가짜 최저가를 거르는 게 이 시스템의 핵심 방어선이다.

**포함 조건** (아래 중 하나라도 상품명에 있어야 함, 공백/대소문자 정규화 후 비교)
```
AirPro S / AirProS / Air Pro S
```

**제외 조건** (하나라도 있으면 탈락)
```
カートリッジ, 替え, 交換, ケース, 収納, カバー, フィルム, 保護,
中古, 未使用品, 訳あり, ジャンク, 並行輸入, レンタル
```

**가격 하한 가드**
- `SANE_MIN = 18000`. 정가 49,800엔에서 46%OFF가 26,892엔이었으므로, 18,000엔 미만은
  본체일 가능성이 희박 → 부속품/오탐으로 간주하고 제외.
- `SANE_MAX = 80000`. 세트 상품 등 배제.

**재고 필터**
- 楽天: `availability == 1` 만 채택
- Yahoo: `inStock == true` 만 채택
- 재고 없는 상품이 저가로 걸려 ATL을 오염시키는 걸 막는다

### 4.3 API 장애 / 레이트리밋

- 사이트별로 try/except. **한쪽이 죽어도 다른 쪽 결과로 진행**한다.
- 양쪽 다 실패하면 그 회차는 아무것도 기록하지 않고 종료(exit 0). 실패를 이력에 남기지 않는다.
- 타임아웃 20초, 재시도 2회(지수 백오프 2s → 4s).
- 楽天 API는 1초 1회 제한이 있으므로 요청 간 `sleep(1)`.

### 4.4 파싱 헬스 체크

Ulike가 상품명 표기 방식을 바꾸면 파싱이 **조용히 실패**한다. 에러가 안 나고 그냥
정가만 잡히므로 알림이 영영 안 오게 된다.

- 매 실행마다 `후보 건수`와 `쿠폰 파싱 성공 건수`를 state에 기록
- **3회 연속으로 후보 0건**이면 "검색 결과 없음" 경고 메일 (검색어가 죽었을 가능성)
- **7일 연속으로 쿠폰 파싱 성공 0건**이면 "파싱 패턴 점검 요망" 경고 메일
- 각 경고는 발송 후 쿨다운 7일 (스팸 방지)

### 4.5 모델 단종 / 후속기 출시

AirPro S가 구형이 되면 상시 저가가 되어 ATL이 계속 갱신되며 메일이 잦아진다.
- 자동 대응은 하지 않는다. §2.3의 500엔 억제로 어느 정도 완화됨.
- 메일이 잦아지면 사람이 판단해서 감시를 종료하거나 대상을 바꾸는 것으로 한다.

### 4.6 GitHub Actions 관련

- **cron은 지연된다.** 수 분~수십 분 늦게 도는 게 정상. 정시 보장 안 됨.
- 리포지토리에 60일간 활동이 없으면 스케줄 워크플로가 자동 비활성화된다.
  본 시스템은 매 실행마다 `history.jsonl`을 커밋하므로 활동이 계속 발생 → 문제없음.
- state/history 커밋이 동시 실행으로 충돌하지 않게 `concurrency: price-watch` 지정.
- `permissions: contents: write` 필요.

### 4.7 시크릿 취급

- 시크릿은 전부 GitHub Actions Secrets로. 코드/커밋/로그에 절대 노출 금지.
- 로그 출력 시 URL에 쿼리 파라미터로 키가 섞이지 않게 주의(API 키가 쿼리에 들어가므로
  **예외 메시지에 URL 전문을 그대로 찍지 말 것**).

---

## 5. 설정값 일람

### 5.1 환경변수

| 이름 | 필수 | 설명 |
|---|---|---|
| `RAKUTEN_APP_ID` | △ | 楽天 applicationId |
| `YAHOO_CLIENT_ID` | △ | Yahoo Client ID |
| `GMAIL_USER` | O | 발신 주소 |
| `GMAIL_APP_PASSWORD` | O | 앱 패스워드 16자리 |
| `MAIL_TO` | - | 수신 주소 (미지정시 `GMAIL_USER`) |

△: 둘 중 최소 하나 필수.

### 5.2 상수

```python
MIN_DROP_YEN = 500      # 이 폭 미만 갱신은 메일 억제
SANE_MIN     = 18000    # 실질가 하한 (오탐 가드)
SANE_MAX     = 80000    # 실질가 상한
TIMEOUT_SEC  = 20
RETRY        = 2
```

### 5.3 스케줄

`0 0,4,12 * * *` (UTC) = JST 09:00 / 13:00 / 21:00

---

## 6. 파일 구성

```
price-watch/
├── watch.py                        # 본체
├── README.md                       # 셋업 순서
├── docs/
│   ├── index.html                  # 대시보드 (GitHub Pages)
│   └── history.jsonl               # 가격 이력 (자동 생성/커밋)
├── state.json                      # ATL, 헬스 카운터 (자동 생성/커밋)
└── .github/workflows/price-watch.yml
```

---

## 7. 구현 시 확인할 것

Claude Code가 실제로 돌려보며 검증해야 하는 항목:

1. **楽天 API 실제 응답 확인** — `itemName`에 쿠폰 표기가 실제로 들어오는지.
   본 사양의 파싱 패턴은 검색 결과에서 관찰한 표기를 근거로 했으나, API 응답의
   실제 문자열은 확인되지 않았다. 다르면 패턴을 고칠 것.
2. **`must_include` 필터가 과하지 않은지** — 정규화 후에도 매칭이 0건이면 표기 흔들림
   (전각/반각 공백, `AirProS` 붙여쓰기 등)을 흡수하도록 완화.
3. **재고 필드명** — 楽天 `availability`, Yahoo `inStock`의 실제 키 이름과 타입 확인.
4. **비세일 기간의 동작** — 쿠폰이 없는 시기에는 정가 49,800엔이 잡혀야 정상.
5. **`--dry-run` 이 state/history를 건드리지 않는지.**

## 8. 명시적 비목표

- 자동 구매, 장바구니 담기 → 하지 않는다
- Amazon / メルカリ 수집 → 하지 않는다
- 포인트 환원 계산 → 하지 않는다
- 여러 모델 비교 → 지금은 하지 않는다 (구조만 남겨둠)
